<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
<title>入力結果</title>
</head>
<body>
  <%
  require 'mysql2'

  client = Mysql2::Client.new(:host => 'localhost', :username => 'root', :password => 'root', :database => 'GEEKJOB_db')

  # client.query(%q{update profiles set name = '@params["name"]' tel = '@params["tel"]' age = @params["age"] birthday = '@params["birthday"]' where profilesID = @params["id"]})
  # statement = client.prepare(%q{update profiles set (name, tel, age, birthday) VALUES (?, ?, ?, ?) where profilesID = '?'})
  statement = client.prepare(%q{update profiles set name = ?, tel = ?, age = ?, birthday = ? where profilesID = ?})
  statement.execute(@params["name"],@params["tel"],@params["age"],@params["birthday"],@params["id"])

  # statement = client.prepare(%q{update profiles set name = ?, tel = ?,  where profilesID = ?})
  # statement.execute(@params["name"],@params["tel"],@params["id"])
 %>
<p>DBへの追加完了</p>
</body>
</html>
